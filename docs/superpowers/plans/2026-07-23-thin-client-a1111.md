# Thin-Client 0.5.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Das Plugin wird Thin-Client zu einem A1111-kompatiblen lokalen Bild-Server (Draw Things, A1111, Forge, SD.Next); alle vier Store-Findings (fs/child_process/eval/>5MB) fallen.

**Architecture:** ORT-Pipeline + mflux-Kindprozess werden durch einen puren `Txt2ImgClient` (injiziertes HTTP über `requestUrl`) ersetzt. Produkt-Schicht (Hub, Presets, Historie, Notizen, i18n) bleibt. Spec: `docs/superpowers/specs/2026-07-23-thin-client-a1111-design.md`.

**Tech Stack:** TypeScript, esbuild, vitest (Node + happy-dom), Obsidian-API (`requestUrl`), eslint-plugin-obsidianmd.

## Global Constraints

- **Gate vor jedem Commit grün:** `npm run gate` (typecheck + vitest + eslint/check:pure + build).
- **Pure-Core-Schnitt:** `src/core/` und `src/vendor/kit/` importieren NIE `obsidian` (check-pure.mjs).
- **Alle UI-Strings über i18n** (`t("key")`), EN **und** DE pflegen (`src/i18n/strings.ts`).
- **Timer nur `window.setTimeout`/`window.clearTimeout`** (Store-Scanner), DOM nur über `createEl`/`createDiv` — nie `document.createElement`.
- **Conventional Commits deutsch** mit `Co-Authored-By: Claude <noreply@anthropic.com>`-Trailer (bzw. Modell-Trailer des Executors).
- **Kein `fs`, kein `child_process`, kein `eval`, kein Prozess-Start — nirgends, auch nicht "übergangsweise".**
- Vitest: pure Tests ohne Environment-Pragma; obsidian-Layer-Tests mit `// @vitest-environment happy-dom` (Muster `tests/timeout.test.ts`).
- Referenz-Implementierung für den Client: `../yijing-oracle/src/obsidian/image-client.ts` + `http.ts` (`probeImageEndpoint` existiert dort bereits).

---

### Task 1: Pure Txt2Img-Client + Endpoint-Vendoring

**Files:**
- Create: `src/vendor/kit/endpoint.ts` (Vendoring aus `../obsidian-kit/src/pure/endpoint.ts` — nur `normalizeEndpoint`, Rest YAGNI)
- Create: `src/core/txt2img.ts`
- Test: `tests/txt2img.test.ts`

**Interfaces:**
- Produces: `Txt2ImgClient` (implements `ImageBackend`), `ImageRequest`, `HttpPostJson`, `parseOptionsModel(json): string | null`, `parseProgressPct(json): number | null` — Task 5 verdrahtet sie in main.ts.

- [ ] **Step 1: Vendoring** — `src/vendor/kit/endpoint.ts` anlegen (Kommentar-Kopf `// vendored from obsidian-kit, src/pure/endpoint.ts` + nur die `normalizeEndpoint`-Funktion, Wortlaut identisch zur yijing-Kopie `../yijing-oracle/src/vendor/kit/endpoint.ts:9-11`).

- [ ] **Step 2: Failing Tests schreiben** — `tests/txt2img.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { parseOptionsModel, parseProgressPct, Txt2ImgClient } from "../src/core/txt2img";

const req = { prompt: "a cat", negativePrompt: "blurry", width: 768, height: 512, steps: 20, seed: 42, cfg: 7 };

describe("Txt2ImgClient", () => {
  it("mappt das Rezept auf den A1111-Body und liefert images[0]", async () => {
    let captured: { url: string; body: unknown } | null = null;
    const client = new Txt2ImgClient("http://127.0.0.1:7860/", async (url, body) => {
      captured = { url, body };
      return { status: 200, json: { images: ["BASE64PNG"] } };
    });
    const png = await client.generate(req);
    expect(png).toBe("BASE64PNG");
    expect(captured!.url).toBe("http://127.0.0.1:7860/sdapi/v1/txt2img");
    expect(captured!.body).toEqual({
      prompt: "a cat", negative_prompt: "blurry", width: 768, height: 512,
      steps: 20, seed: 42, cfg_scale: 7,
    });
  });
  it("wirft Klartext bei HTTP != 200", async () => {
    const client = new Txt2ImgClient("http://x", async () => ({ status: 500, json: {} }));
    await expect(client.generate(req)).rejects.toThrow("txt2img HTTP 500");
  });
  it("wirft bei leerem/fehlendem images", async () => {
    const client = new Txt2ImgClient("http://x", async () => ({ status: 200, json: { images: [] } }));
    await expect(client.generate(req)).rejects.toThrow("txt2img: empty result");
  });
});

describe("parseOptionsModel", () => {
  it("liest sd_model_checkpoint", () => {
    expect(parseOptionsModel({ sd_model_checkpoint: "flux.2-klein" })).toBe("flux.2-klein");
  });
  it("null bei fremder Form", () => {
    expect(parseOptionsModel({})).toBeNull();
    expect(parseOptionsModel(null)).toBeNull();
    expect(parseOptionsModel("x")).toBeNull();
  });
});

describe("parseProgressPct", () => {
  it("skaliert progress 0..1 auf ganze Prozent", () => {
    expect(parseProgressPct({ progress: 0.42 })).toBe(42);
  });
  it("null bei fehlendem/kaputtem Feld", () => {
    expect(parseProgressPct({})).toBeNull();
    expect(parseProgressPct(null)).toBeNull();
    expect(parseProgressPct({ progress: "x" })).toBeNull();
  });
});
```

- [ ] **Step 3: Fail bestätigen** — `npx vitest run tests/txt2img.test.ts` → FAIL (Modul fehlt).

- [ ] **Step 4: Implementierung** — `src/core/txt2img.ts`:

```ts
// A1111-kompatibler txt2img-Client (Spec §3) — pure, HTTP injiziert; Referenz:
// yijing-oracle/src/obsidian/image-client.ts. Deckt Draw Things, A1111, Forge, SD.Next.
import { normalizeEndpoint } from "../vendor/kit/endpoint";

export type HttpPostJson = (url: string, body: unknown) => Promise<{ status: number; json: unknown }>;

export interface ImageRequest {
  prompt: string;
  negativePrompt: string;
  width: number;
  height: number;
  steps: number;
  seed: number;
  cfg: number;
}

export interface ImageBackend {
  /** Liefert das Bild als Base64-PNG; wirft Error mit Klartext bei Fehlschlag. */
  generate(req: ImageRequest): Promise<string>;
}

export class Txt2ImgClient implements ImageBackend {
  constructor(
    private readonly endpoint: string,
    private readonly post: HttpPostJson,
  ) {}

  async generate(req: ImageRequest): Promise<string> {
    const url = `${normalizeEndpoint(this.endpoint)}/sdapi/v1/txt2img`;
    const { status, json } = await this.post(url, {
      prompt: req.prompt,
      negative_prompt: req.negativePrompt,
      width: req.width,
      height: req.height,
      steps: req.steps,
      seed: req.seed,
      cfg_scale: req.cfg,
    });
    if (status !== 200) throw new Error(`txt2img HTTP ${status}`);
    const images = (json as { images?: unknown })?.images;
    const first: unknown = Array.isArray(images) ? images[0] : undefined;
    if (typeof first !== "string" || !first) throw new Error("txt2img: empty result");
    return first;
  }
}

/** Aktives Modell aus GET /sdapi/v1/options — null, wenn der Server das Feld nicht liefert
 *  (Draw Things liefert die Options-Form nur teilweise; null → UI zeigt "(im Server gewählt)"). */
export function parseOptionsModel(json: unknown): string | null {
  const v = (json as { sd_model_checkpoint?: unknown } | null)?.sd_model_checkpoint;
  return typeof v === "string" && v !== "" ? v : null;
}

/** Fortschritt aus GET /sdapi/v1/progress als ganze Prozent — null bei fremder Form
 *  (Fallback der Statuszeile: Sekundenzähler statt Prozent, Spec §3). */
export function parseProgressPct(json: unknown): number | null {
  const v = (json as { progress?: unknown } | null)?.progress;
  return typeof v === "number" && Number.isFinite(v) ? Math.round(v * 100) : null;
}
```

- [ ] **Step 5: Pass bestätigen** — `npx vitest run tests/txt2img.test.ts` → PASS. Dann `npm run gate` → grün.
- [ ] **Step 6: Commit** — `feat(core): A1111-Txt2Img-Client + Options-/Progress-Parser (Spec §3)`

---

### Task 2: Generations-Konstanten + Settings-Umbau (pure)

**Files:**
- Create: `src/core/generation.ts`
- Modify: `src/core/settings.ts`
- Test: `tests/generation.test.ts`, `tests/settings.test.ts` (erweitern)

**Interfaces:**
- Produces: `SIZES: readonly SizeOption[]` (die 7 Größen aus dem alten FLUX-Katalog), `SizeOption`, `STEPS = { min: 1, max: 50, default: 20 }`, `CFG = { min: 1, max: 15, step: 0.5, default: 7 }`; `LigSettings.endpoint: string`; tote Keys `mfluxPath`/`modelsDir`/`selectedModel` (bleiben ladbar, kein Codepfad liest sie).

- [ ] **Step 1: Failing Tests** — `tests/generation.test.ts` (SIZES enthält 7 Einträge, alle Vielfache von 16; STEPS/CFG-Grenzen wie oben) und in `tests/settings.test.ts` ergänzen: `sanitizeSettings({})` → `endpoint === ""`; `sanitizeSettings({ endpoint: 42 })` → `""`; `sanitizeSettings({ defaultSteps: 20 })` → `20`; `defaultSteps: 51` → Default `20`; alte Keys `mfluxPath`/`modelsDir`/`selectedModel` überleben als Strings (Muster `sectionsCollapsed`).
- [ ] **Step 2: Fail bestätigen.**
- [ ] **Step 3: Implementierung** — `src/core/generation.ts`:

```ts
// Generations-Grenzen (Spec §4) — ersetzen den Modell-Katalog (models.ts stirbt in Task 8):
// der Server hält die Modelle, das Plugin bietet generische, ehrliche Regler.
export interface SizeOption { width: number; height: number; }

export const SIZES: readonly SizeOption[] = [
  { width: 512, height: 512 },
  { width: 768, height: 768 },
  { width: 1024, height: 1024 },
  { width: 768, height: 512 },
  { width: 512, height: 768 },
  { width: 1024, height: 576 },
  { width: 576, height: 1024 },
];
export const DEFAULT_SIZE: SizeOption = SIZES[0]!;

export const STEPS = { min: 1, max: 50, default: 20 } as const;
export const CFG = { min: 1, max: 15, step: 0.5, default: 7 } as const;
```

  In `src/core/settings.ts`: Import von `models.ts` entfernen; `endpoint: string` zu `LigSettings` + `DEFAULT_SETTINGS` (`""`) + `sanitizeFolder`-artiger String-Sanitizer; `sanitizeDefaultSteps` auf `1..50`, Default `20` (`DEFAULT_SETTINGS.defaultSteps: 20`, Import `STEPS` aus `generation.ts` verwenden); JSDoc der Felder `mfluxPath`/`modelsDir`/`selectedModel` auf „Legacy, toter Key seit 0.5 (Thin-Client)" ändern, `sanitizeSelectedModel` durch schlichten String-Sanitizer ersetzen (kein `MODELS`-Bezug mehr). `HistoryEntry` bleibt hier unangetastet (Task 3).
- [ ] **Step 4: Pass + Gate.** *(models.ts existiert noch und wird noch von main/panels importiert — Gate bleibt grün, weil settings.ts es nur nicht mehr braucht.)*
- [ ] **Step 5: Commit** — `feat(core): Generations-Konstanten + endpoint-Setting, mflux/Modell-Keys tot (Spec §4/§5)`

---

### Task 3: Rezept-Erweiterung negativePrompt + cfg (pure)

**Files:**
- Modify: `src/core/settings.ts` (`HistoryEntry` + `sanitizeHistory`), `src/core/history.ts` (`recipeKey`, `deleteEntry`), `src/core/viewmodel.ts` (nur `GenParams`), `src/core/note.ts`
- Test: `tests/history.test.ts`, `tests/settings.test.ts`, `tests/note.test.ts` (erweitern)

**Interfaces:**
- Produces: `HistoryEntry` + `GenParams` je `+ negativePrompt: string; + cfg: number` — Migration fehlender Felder → `""` / `7`. Frontmatter: `negative_prompt` nur wenn nicht leer, `cfg` immer.

- [ ] **Step 1: Failing Tests** — history: zwei Einträge, die sich NUR in `negativePrompt` (bzw. nur in `cfg`) unterscheiden, kollabieren nicht (`pushHistory`) und `deleteEntry` trifft exakt; settings: `sanitizeHistory` füllt Alt-Einträge mit `negativePrompt: ""` / `cfg: 7`; note: Frontmatter enthält `cfg: 7` immer und `negative_prompt` nur bei nicht-leerem Wert, Reihenfolge `prompt, negative_prompt, seed, steps, cfg, model, width, height, created, image`.
- [ ] **Step 2: Fail bestätigen.**
- [ ] **Step 3: Implementierung** — `HistoryEntry`/`GenParams` um die zwei Felder ergänzen; `recipeKey`-Tupel um `e.negativePrompt.trim()` und `e.cfg` erweitern (ans Ende); `deleteEntry`-Gleichheit um beide Felder; `sanitizeHistory`-Map um `negativePrompt: typeof h.negativePrompt === "string" ? h.negativePrompt : ""` und `cfg: typeof h.cfg === "number" ? h.cfg : 7`; `note.ts`:

```ts
const FM_ORDER = ["prompt", "negative_prompt", "seed", "steps", "cfg", "model", "width", "height", "created", "image"];
// im Builder:
const data: Record<string, FmValue> = {
  prompt: params.prompt,
  ...(params.negativePrompt.trim() !== "" ? { negative_prompt: params.negativePrompt } : {}),
  seed: params.seed,
  steps: params.steps,
  cfg: params.cfg,
  model: params.model,
  width: params.width,
  height: params.height,
  created: params.date,
  image: `[[${imageLink}]]`,
};
```

  *(Achtung: `viewmodel.ts` kompiliert nach der `GenParams`-Erweiterung nur, wenn alle `params:`-Literalstellen in `main.ts` mitgezogen werden — in diesem Task NICHT anfassen; stattdessen in `main.ts` an den zwei `params: {...}`-Stellen übergangsweise `negativePrompt: "", cfg: 7` ergänzen, Task 5 ersetzt beide Stellen ohnehin.)*
- [ ] **Step 4: Pass + Gate.**
- [ ] **Step 5: Commit** — `feat(core): Rezepte tragen negativePrompt + cfg (Spec §5)`

---

### Task 4: ViewModel-Neuschnitt (pure)

**Files:**
- Modify: `src/core/viewmodel.ts` (kompletter Neuschnitt der State-Typen + buildViewModel; `formatElapsed` bleibt, `formatBytes` stirbt, `GpuState`/`ModelState`/`MfluxPanelState`/`MFLUX_SLOW_LOAD_HINT_SEC` sterben)
- Test: `tests/viewmodel.test.ts` (Neuschrieb)

**Interfaces:**
- Produces (Task 5–7 verdrahten):

```ts
export type ServerState =
  | { kind: "unconfigured" }
  | { kind: "checking" }
  | { kind: "ok"; modelName: string | null }
  | { kind: "unreachable" };
export type RunState =
  | { kind: "idle" }
  | { kind: "contacting" }
  | { kind: "generating"; pct: number | null; elapsedSec: number }
  | { kind: "error"; message: string };
export interface PanelState {
  server: ServerState;
  run: RunState;
  image: { dataUrl: string; params: GenParams } | null;
  editorActive: boolean;
  prompt: string;
  negativePrompt: string;
  seed: number;
  steps: number;
  cfg: number;
  width: number;
  height: number;
}
// PanelViewModel unverändert (status/empty/generateEnabled/insertEnabled/showImage);
// empty bekommt zusätzlich das optionale Feld ctaAction: "settings" | "recheck".
```

- [ ] **Step 1: Failing Tests** (Neuschrieb, die alten ORT-/mflux-Fälle ersatzlos raus). Muss abdecken:
  - `unconfigured` → empty mit `ctaAction: "settings"`, generateEnabled false, Status `is-error`.
  - `checking` → Status loader, generateEnabled false.
  - `unreachable` → empty mit `ctaAction: "recheck"`, generateEnabled false.
  - `ok` + idle + Prompt nicht leer → generateEnabled true, Status `is-ok` (`status.ready`).
  - `run.contacting` → Status loader `status.contacting`, generateEnabled false.
  - `run.generating` mit `pct: 42` → Text enthält `42`; mit `pct: null, elapsedSec: 65` → Text enthält `1:05`.
  - `run.error` → Status `is-error` mit Message, generateEnabled true bei nicht-leerem Prompt (Retry über Generate, bestehendes Muster).
  - **Gating:** identisches Rezept wie `image.params` (inkl. negativePrompt+cfg) UND `server.modelName === image.params.model` → generateEnabled false; `modelName: null` → generateEnabled true trotz gleicher Felder (Serverwechsel unbeobachtbar → nie fälschlich sperren); ein abweichendes Feld (z. B. cfg) → true.
- [ ] **Step 2: Fail bestätigen.**
- [ ] **Step 3: Implementierung** — ein `buildViewModel` ohne Modell-Weiche:

```ts
function recipeUnchanged(s: PanelState): boolean {
  const p = s.image?.params;
  return (
    p !== undefined &&
    s.server.kind === "ok" &&
    s.server.modelName !== null &&
    s.server.modelName === p.model &&
    p.prompt === s.prompt &&
    p.negativePrompt === s.negativePrompt &&
    p.seed === s.seed &&
    p.steps === s.steps &&
    p.cfg === s.cfg &&
    p.width === s.width &&
    p.height === s.height
  );
}

export function buildViewModel(s: PanelState): PanelViewModel {
  const busy = s.run.kind === "contacting" || s.run.kind === "generating";

  let status: PanelViewModel["status"];
  if (s.run.kind === "error") status = { icon: "circle-x", text: t("status.error", s.run.message), cls: "is-error" };
  else if (s.server.kind === "unconfigured") status = { icon: "circle-x", text: t("status.noEndpoint"), cls: "is-error" };
  else if (s.server.kind === "checking") status = { icon: "loader", text: t("status.serverChecking"), cls: "is-checking" };
  else if (s.server.kind === "unreachable") status = { icon: "circle-x", text: t("status.serverUnreachable"), cls: "is-error" };
  else if (s.run.kind === "contacting") status = { icon: "loader", text: t("status.contacting"), cls: "is-checking" };
  else if (s.run.kind === "generating")
    status =
      s.run.pct !== null
        ? { icon: "loader", text: t("status.generatingPct", s.run.pct), cls: "is-checking" }
        : { icon: "loader", text: t("status.generatingElapsed", formatElapsed(s.run.elapsedSec)), cls: "is-checking" };
  else status = { icon: "circle-check", text: t("status.ready"), cls: "is-ok" };

  let empty: PanelViewModel["empty"] = null;
  if (s.server.kind === "unconfigured")
    empty = { text: t("empty.noServer"), ctaLabel: t("empty.noServerCta"), ctaAction: "settings" };
  else if (s.server.kind === "unreachable")
    empty = { text: t("empty.unreachable"), ctaLabel: t("empty.unreachableCta"), ctaAction: "recheck" };
  else if (!s.image && !busy) empty = { text: t("empty.noImage") };

  return {
    status,
    empty,
    generateEnabled: s.server.kind === "ok" && !busy && s.prompt.trim().length > 0 && !recipeUnchanged(s),
    insertEnabled: s.image !== null && s.editorActive && !busy,
    showImage: s.image !== null,
  };
}
```

  *(Kompiliert erst zusammen mit den i18n-Keys aus diesem Task: die neuen Keys `status.noEndpoint`, `status.serverChecking`, `status.serverUnreachable`, `status.contacting`, `status.generatingPct`, `status.generatingElapsed`, `empty.noServer`, `empty.noServerCta`, `empty.unreachable`, `empty.unreachableCta` in EN+DE in `src/i18n/strings.ts` anlegen — EN-Wortlaute: "No image server configured" · "Checking server…" · "Server unreachable — is the API enabled?" · "Contacting server…" · "Generating… {0}%" · "Generating… ({0})" · "Connect a local image server such as Draw Things (enable its API server) or AUTOMATIC1111 (--api), then enter the endpoint in the settings." · "Open settings" · "The server did not respond. Is it running and the API enabled?" · "Retry" — DE sinngleich mit korrekten Umlauten. Alte ORT-/mflux-/Download-Keys erst in Task 8 löschen, sonst bricht der noch nicht umgebaute Rest.)*
  *(`tests/viewmodel.test.ts`-Neuschrieb braucht `registerI18n()` wie der Bestand; `main.ts`/Panels kompilieren nach dem State-Umbau NICHT mehr — deshalb gehören Task 4 und 5 in DENSELBEN PR-Abschnitt: Task 4 committet nur, wenn `npm run typecheck` grün ist; ist das wegen main.ts unmöglich, Task 4+5 als EIN Commit am Ende von Task 5.)*
- [ ] **Step 4: `npx vitest run tests/viewmodel.test.ts` → PASS.**
- [ ] **Step 5: Commit (ggf. gemeinsam mit Task 5, s. o.)** — `feat(core): ViewModel auf Server-/Run-State umgeschnitten (Spec §3/§4)`

---

### Task 5: HTTP-Layer + main.ts-Umbau

**Files:**
- Create: `src/obsidian/http.ts`
- Modify: `src/main.ts`, `src/obsidian/view.ts` (ViewHost-Signaturen), `src/obsidian/png.ts` (auf `dataUrlToBytes` reduzieren — Spec-§6-Abweichung: die Funktion bleibt nötig)
- Test: keiner neu (dünner requestUrl-Wrapper, Muster yijing/http.ts ungetestet; Logik steckt in Task 1/4)

**Interfaces:**
- Consumes: `Txt2ImgClient`, `parseOptionsModel`, `parseProgressPct` (Task 1); `ServerState`/`RunState`/`PanelState` (Task 4); `SIZES`/`STEPS`/`CFG` (Task 2).
- Produces: `httpPostJson(url, body, timeoutMs?)` und `httpGetJson(url, timeoutMs?)`; `ViewHost` neu: `generate(steps, seed, cfg, width, height)` (negativePrompt läuft wie prompt über `setNegativePrompt`), `setRecipe(steps, seed, cfg, width, height)`, `setNegativePrompt(p)`, `recheckServer()`; `setSelectedModel`/`downloadModel`/`downloadFluxModel` entfallen.

- [ ] **Step 1: `src/obsidian/http.ts`** (nach `../yijing-oracle/src/obsidian/http.ts:18-37`, aber ohne Kit-Diagnostics — YAGNI):

```ts
// Netz-Helfer über Obsidians requestUrl (CORS-frei) — kapselt den obsidian-Import,
// damit Txt2ImgClient pure bleibt. Muster: yijing-oracle/src/obsidian/http.ts.
import { requestUrl } from "obsidian";

/** Passt zu HttpPostJson in core/txt2img.ts. Default-Timeout 30 min: FLUX auf schwacher
 *  Hardware darf Minuten dauern (Spec §3) — der Timeout ist nur die Notbremse gegen
 *  ewiges Hängen, requestUrl kennt weder timeout noch Abort. */
export async function httpPostJson(url: string, body: unknown, timeoutMs = 1_800_000): Promise<{ status: number; json: unknown }> {
  let timer: number | undefined;
  const timeout = new Promise<"__timeout__">((resolve) => {
    timer = window.setTimeout(() => resolve("__timeout__"), timeoutMs);
  });
  try {
    const raced = await Promise.race([
      requestUrl({ url, method: "POST", contentType: "application/json", body: JSON.stringify(body), throw: false }).then((r) => {
        let json: unknown = undefined;
        try { json = r.json; } catch { /* nicht-JSON-Body → json bleibt undefined */ }
        return { status: r.status, json } as const;
      }),
      timeout,
    ]);
    if (raced === "__timeout__") throw new Error(`timeout after ${timeoutMs} ms`);
    return raced;
  } finally {
    if (timer !== undefined) window.clearTimeout(timer);
  }
}

/** GET mit kurzem Timeout — für options-/progress-Discovery (Spec §3: 3 s). */
export async function httpGetJson(url: string, timeoutMs = 3000): Promise<{ status: number; json: unknown }> {
  let timer: number | undefined;
  const timeout = new Promise<"__timeout__">((resolve) => {
    timer = window.setTimeout(() => resolve("__timeout__"), timeoutMs);
  });
  try {
    const raced = await Promise.race([
      requestUrl({ url, throw: false }).then((r) => {
        let json: unknown = undefined;
        try { json = r.json; } catch { /* nicht-JSON-Body → json bleibt undefined */ }
        return { status: r.status, json } as const;
      }),
      timeout,
    ]);
    if (raced === "__timeout__") throw new Error(`timeout after ${timeoutMs} ms`);
    return raced;
  } finally {
    if (timer !== undefined) window.clearTimeout(timer);
  }
}
```

- [ ] **Step 2: main.ts umbauen.** Kern-Änderungen (die gesamte ORT-/mflux-Verdrahtung fliegt: Imports von `engine`, `model-manifest`, `models`, `mflux-host`, `mflux-engine`, `model-store`, `ort-host`, `rgbaToDataUrl`; Felder `modelStore`/`engine`/`mfluxEngine`/`engineLoadGeneration`; Methoden `initStatus` (GPU/Model-Teil), `downloadModel`, `downloadFluxModel`, `refreshMfluxStatus`, `loadEngine`, `ensureEngine`, `generateOrt`, `generateMflux`, `onModelDeleted`, der `unhandledrejection`-Block, die mflux-/dispose-Teile von `onunload`):
  - State-Init: `server: { kind: this.settings.endpoint.trim() === "" ? "unconfigured" : "checking" }, run: { kind: "idle" }, image: null, editorActive: false, prompt: "", negativePrompt: "", seed: 0, steps: 20, cfg: 7, width: 512, height: 512`.
  - Neue Methode `checkServer()` (onload nach registerView aufrufen + von `recheckServer`-Host + nach Endpoint-Änderung aus dem Settings-Tab):

```ts
/** Erreichbarkeit + aktives Modell in den State spiegeln (Spec §3: GET /sdapi/v1/options,
 *  200 ohne Modellfeld gilt als OK — Draw Things liefert die Options-Form nur teilweise). */
async checkServer(): Promise<void> {
  const ep = this.settings.endpoint.trim();
  if (ep === "") {
    this.state.server = { kind: "unconfigured" };
    this.refreshViews();
    return;
  }
  this.state.server = { kind: "checking" };
  this.refreshViews();
  try {
    const r = await httpGetJson(`${normalizeEndpoint(ep)}/sdapi/v1/options`);
    this.state.server = r.status === 200 ? { kind: "ok", modelName: parseOptionsModel(r.json) } : { kind: "unreachable" };
  } catch {
    this.state.server = { kind: "unreachable" };
  }
  this.refreshViews();
}
```

  - Neue `generate(steps, seed, cfg, width, height)` (ersetzt Router + beide Engine-Methoden; Struktur des Erfolgs-/Fehler-/History-Pfads aus `generateMflux` übernehmen — eingefrorener Prompt, `succeeded`-Flag, `pushHistory` außerhalb des try):

```ts
private async generate(steps: number, seed: number, cfg: number, width: number, height: number): Promise<void> {
  if (this.state.run.kind === "contacting" || this.state.run.kind === "generating") return;
  if (this.state.server.kind !== "ok") return; // ViewModel gated das bereits — Defensive
  const prompt = this.state.prompt;
  const negativePrompt = this.state.negativePrompt;
  const model = this.state.server.modelName ?? "unknown";
  const client = new Txt2ImgClient(this.settings.endpoint, httpPostJson);
  this.state.run = { kind: "contacting" };
  this.refreshViews();
  // Fortschritt: 1-s-Polling auf /sdapi/v1/progress; liefert der Server keins (404,
  // Timeout, fremde Form), bleibt pct null und die Statuszeile zählt Sekunden.
  let elapsed = 0;
  const tick = window.setInterval(() => {
    elapsed += 1;
    if (this.state.run.kind !== "generating" && this.state.run.kind !== "contacting") return;
    void httpGetJson(`${normalizeEndpoint(this.settings.endpoint)}/sdapi/v1/progress`, 1000)
      .then((r) => {
        if (this.state.run.kind === "generating" || this.state.run.kind === "contacting")
          this.state.run = { kind: "generating", pct: r.status === 200 ? parseProgressPct(r.json) : null, elapsedSec: elapsed };
        this.refreshViews();
      })
      .catch(() => {
        if (this.state.run.kind === "generating" || this.state.run.kind === "contacting")
          this.state.run = { kind: "generating", pct: null, elapsedSec: elapsed };
        this.refreshViews();
      });
  }, 1000);
  let succeeded = false;
  try {
    const png = await client.generate({ prompt, negativePrompt, width, height, steps, seed, cfg });
    this.state.image = {
      dataUrl: `data:image/png;base64,${png}`,
      params: { prompt, negativePrompt, seed, steps, cfg, model, width, height, date: isoStamp(new Date()) },
    };
    this.state.run = { kind: "idle" };
    succeeded = true;
  } catch (e) {
    const msg = e instanceof Error ? e.message : String(e);
    this.state.run = { kind: "error", message: msg };
    // Fehlschlag kann Erreichbarkeits-Ursache haben → Serverstatus neu prüfen (fire-and-forget).
    void this.checkServer();
  } finally {
    window.clearInterval(tick);
    this.refreshViews();
  }
  if (succeeded && this.state.image) {
    const p = this.state.image.params;
    this.settings.history = pushHistory(this.settings.history, {
      prompt: p.prompt, negativePrompt: p.negativePrompt, seed: p.seed, steps: p.steps,
      cfg: p.cfg, model: p.model, width: p.width, height: p.height, created: p.date,
    });
    void this.saveSettings();
  }
}
```

  - `ViewHost` in `view.ts` anpassen (Signaturen s. Interfaces oben); Host-Implementierung: `setNegativePrompt`, `setRecipe(steps, seed, cfg, width, height)`, `recheckServer: () => void this.checkServer()`. `openSettings`/`restoreRecipe`/History-Host-Teile unverändert.
  - `saveImage`/`resolveImagePath`/`createNote`/`revealFile` unverändert (nutzen `dataUrlToBytes` aus dem reduzierten `png.ts`).
  - `onunload`: bleibt als leere/minimale Methode oder entfällt (kein Prozess, keine Sessions mehr).
- [ ] **Step 3: `png.ts` reduzieren** — nur `dataUrlToBytes` behalten (Header-Kommentar anpassen), `rgbaToDataUrl` + Encoder-Teile löschen. *(Kompiliert erst, wenn kein Import mehr darauf zeigt — genau dieser Task entfernt den letzten.)*
- [ ] **Step 4: `npm run typecheck`** → es dürfen NUR noch Fehler aus `generate-panel.ts`/`settings-tab.ts` übrig sein (alte ViewHost-/State-Nutzung). Diese zwei Dateien minimal auf die neuen Signaturen bringen, damit das Gate läuft — die eigentliche UI kommt in Task 6/7. Wenn „minimal" auf dasselbe hinausläuft wie Task 6/7, stattdessen Task 5+6+7 als eine Commit-Serie im selben Branch-Abschnitt fahren und das Gate erst am Ende von Task 7 verlangen.
- [ ] **Step 5: Gate + Commit** — `feat(obsidian): Thin-Client-Wiring — generate() über /sdapi/v1/txt2img (Spec §3)`

---

### Task 6: Generate-Panel-UI

**Files:**
- Modify: `src/obsidian/generate-panel.ts`, `src/i18n/strings.ts`, `styles.css` (nur falls neue Klassen nötig)

**Interfaces:**
- Consumes: neuen `ViewHost` (Task 5), `SIZES`/`STEPS`/`CFG` (Task 2), `buildViewModel` (Task 4).

- [ ] **Step 1: Umbau** (Mount-once-Muster + Signatur-Konventionen der Datei beibehalten):
  - **Modell-Zeile:** `<select>` raus; stattdessen `modelRow.createSpan({ cls: "lig-model-info" })`, in `refresh()` befüllt: `server.kind === "ok" && modelName !== null` → `t("generate.modelInfo", modelName)`, sonst `t("generate.modelInApp")`. `applyModel`/`rebuildSizeDropdown(spec,…)`-Modellbezug entfällt.
  - **Negative-Prompt:** zweite Textarea unter der Prompt-Zeile (`cls: "lig-prompt lig-negative"`, `rows: "2"`, Placeholder `t("generate.negativePromptPlaceholder")`), Input-Handler → `host.setNegativePrompt(...)` + `refresh()`.
  - **Steps-Slider:** fest `min=STEPS.min, max=STEPS.max`, Startwert `getSettings().defaultSteps`.
  - **CFG-Slider:** neben Steps, `min=CFG.min, max=CFG.max, step=CFG.step`, Startwert `CFG.default`, Wertanzeige wie `stepsValueEl` (Label `t("generate.cfg")`).
  - **Größen-Dropdown:** immer sichtbar, Optionen aus `SIZES` (einmalig in `mount()` — kein Rebuild mehr nötig).
  - **`refresh()`:** `host.setRecipe(steps, seed, cfg, width, height)`; Empty-CTA: `ctaAction === "settings"` → `host.openSettings()`, `"recheck"` → `host.recheckServer()` (ein Button, Handler entscheidet anhand des zuletzt gerenderten ViewModels — Feld im Panel merken).
  - **`applyRecipe(entry)`:** Prompt/negativePrompt/seed/steps (geclampt auf `STEPS`)/cfg (geclampt auf `CFG`)/Größe (nur wenn in `SIZES`, sonst Default) in die Felder; kein Modellwechsel mehr.
  - Neue i18n-Keys (EN+DE): `generate.negativePrompt` ("Negative prompt"/"Negativ-Prompt"), `generate.negativePromptPlaceholder` ("What to avoid…"/"Was vermieden werden soll…"), `generate.cfg` ("Guidance (CFG)"/"Guidance (CFG)"), `generate.modelInfo` ("Model: {0}"/"Modell: {0}"), `generate.modelInApp` ("Model: (chosen in the server app)"/"Modell: (in der Server-App gewählt)").
- [ ] **Step 2: Gate** (vitest deckt das Panel nicht — Repo-Muster: kein Obsidian-Mock; ViewModel-Logik ist in Task 4 getestet).
- [ ] **Step 3: Commit** — `feat(ui): Generate-Panel — Negativ-Prompt, CFG, generische Regler, Modell-Anzeige (Spec §4)`

---

### Task 7: Settings-Tab-UI + Legacy-Cache-Aufräumer

**Files:**
- Modify: `src/obsidian/settings-tab.ts`, `src/i18n/strings.ts`
- Create: `src/obsidian/legacy-cache.ts`

**Interfaces:**
- Consumes: `checkServer()` (Task 5), `httpGetJson`, `parseOptionsModel` (Task 1/5).
- Produces: `hasLegacyCache(): Promise<boolean>`, `deleteLegacyCache(): Promise<void>`.

- [ ] **Step 1: `legacy-cache.ts`** — Cache-Namen als Konstante aus dem noch existierenden `model-store.ts` übernehmen (VOR Task 8 hierher kopieren, Quelle im Kommentar nennen):

```ts
// Aufräumer für den 0.x-In-Process-Cache (Spec §4): die SD-Turbo-Gewichte (~2,5 GB) liegen
// bei Bestandsinstallationen noch in der Cache API. Name übernommen aus model-store.ts (0.4).
const LEGACY_CACHE = "<WERT AUS model-store.ts ÜBERNEHMEN>";

export async function hasLegacyCache(): Promise<boolean> {
  try { return await caches.has(LEGACY_CACHE); } catch { return false; }
}
export async function deleteLegacyCache(): Promise<void> {
  try { await caches.delete(LEGACY_CACHE); } catch { /* Best-Effort — Cache API kann in Testumgebungen fehlen */ }
}
```

- [ ] **Step 2: Settings-Tab umbauen:**
  - **Weg:** Modell-Download-Sektion (`refreshModel()`-Mechanik), mflux-Pfad, Modell-Speicherort, Modell-Lösch-Row. Prüfen, ob der file-scoped `prefer-setting-definitions`-Override danach noch nötig ist (Begründung war NUR das partielle Re-Rendering der Download-Zeilen) — wenn nicht: Override entfernen. `refreshModel()` wird zur No-op bzw. entfällt; Aufrufstelle `refreshViews()` in main.ts mitziehen.
  - **Neu, Sektion „Server":** Text-Setting `endpoint` (Name `t("settings.server.name")`, Desc `t("settings.server.desc")`, Placeholder `http://127.0.0.1:7860`; onChange: speichern + `void plugin.checkServer()`) + Button `t("settings.server.test")` (ruft `plugin.checkServer()` und zeigt danach Notice: `server.kind === "ok"` → `t("notice.serverOk", modelName ?? "–")`, sonst `t("notice.serverFail")`).
  - **Legacy-Row:** beim `display()` `void hasLegacyCache().then(...)` — wenn true, Row mit Button `t("settings.legacy.delete")` → `deleteLegacyCache()` + Notice `t("settings.legacy.done")` + Row entfernen. Zusätzlich in `main.ts` `onload`: einmalig pro Session `hasLegacyCache()` → Notice `t("notice.legacyHint")`.
  - **defaultSteps-Slider:** Range 1–50.
  - Neue i18n-Keys (EN+DE): `settings.server.name` ("Server endpoint"/"Server-Endpoint"), `settings.server.desc` ("A1111-compatible local image server — Draw Things (enable API server), AUTOMATIC1111 (--api), Forge, SD.Next."/DE sinngleich), `settings.server.test` ("Test connection"/"Verbindung testen"), `notice.serverOk` ("Server OK — model: {0}"/"Server OK — Modell: {0}"), `notice.serverFail` ("Server not reachable. Check that it is running and the API is enabled."/DE), `settings.legacy.delete` ("Delete old SD-Turbo weights (~2.5 GB)"/"Alte SD-Turbo-Gewichte löschen (~2,5 GB)"), `settings.legacy.done` ("Old weights deleted."/"Alte Gewichte gelöscht."), `notice.legacyHint` ("Old in-process model weights found (~2.5 GB). You can delete them in the settings."/DE).
- [ ] **Step 3: Gate + Commit** — `feat(settings): Server-Endpoint + Test, Download-/mflux-UI raus, Legacy-Cache-Aufräumer (Spec §4)`

---

### Task 8: Maschinenraum-Löschung

**Files:**
- Delete: `src/core/engine.ts`, `src/core/pipeline/` (komplett), `src/core/model-manifest.ts`, `src/core/models.ts`, `src/core/mflux-args.ts`, `src/core/mflux-detect.ts`, `src/core/mflux-output.ts`, `src/obsidian/ort-host.ts`, `src/obsidian/model-store.ts`, `src/obsidian/mflux-engine.ts`, `src/obsidian/mflux-host.ts`, `src/types/wasm.d.ts`
- Delete (Tests): `tests/engine.test.ts`, `f16`, `image`, `prng`, `scheduler`, `tokenizer`, `mflux-args`, `mflux-detect`, `mflux-engine`, `mflux-output`, `model-manifest`, `model-store`, `models` (je `.test.ts`)
- Modify: `esbuild.config.mjs` (`.wasm`-Loader raus; `external`-Liste: `node:*` bleibt nur, falls noch gebraucht — prüfen und sonst mit raus), `package.json` (`onnxruntime-web` aus dependencies), `src/i18n/strings.ts` (tote Keys: alle `status.checking/noWebgpu/noF16/downloading/loadingGpu/engineLoadFailed/loadingMflux*/mfluxMissing/generating`, `empty.noModel*/flux*`, mflux-/Download-Settings-Keys, `notice.oomHint` — in EN und DE)
- Run: `npm uninstall onnxruntime-web`

- [ ] **Step 1: Löschen + Config-Anpassung** (Liste oben vollständig abarbeiten; danach `grep -rn "mflux\|ort-host\|model-store\|model-manifest\|models\"\|pipeline/" src/` → keine Treffer außer ggf. Kommentar-Erwähnungen, die mitzubereinigen sind).
- [ ] **Step 2: Gate** — voller `npm run gate`; erwartete Testzahl deutlich unter 205 (ehrlich sinken lassen, Spec §9).
- [ ] **Step 3: `ls -la main.js`** nach dem Build: **< 2 MB erwartet** (Richtwert < 300 KB).
- [ ] **Step 4: Commit** — `refactor!: In-Process-ORT und mflux-Kindprozess entfernt — Plugin ist Thin-Client (Spec §6)`

---

### Task 9: Qualitäts-Gates gegen Rückfall

**Files:**
- Create: `scripts/check-clean.mjs`
- Modify: `package.json` (`"check:clean": "node scripts/check-clean.mjs"`, `gate` erweitert: `npm run typecheck && npm test && npm run lint && npm run build && npm run check:clean` — NACH build, prüft das Artefakt)

- [ ] **Step 1: Script** — `scripts/check-clean.mjs`:

```js
// Store-Scanner-Vorwegnahme (Spec §7): main.js darf keine System-Fähigkeiten tragen und
// muss klein bleiben. Läuft NACH esbuild gegen das Artefakt (nicht src/): genau das prüft
// auch der Scanner. Lesson „Scanner ≠ lokaler Lint" (_docs/LESSONS.md 2026-07-23).
import { readFileSync, statSync } from "node:fs";

const MAX_BYTES = 2 * 1024 * 1024;
const FORBIDDEN = [/require\(["']child_process["']\)/, /require\(["']fs["']\)/, /require\(["']original-fs["']\)/, /\beval\(/, /new Function\(/];

const size = statSync("main.js").size;
if (size > MAX_BYTES) {
  console.error(`check-clean: main.js ist ${(size / 1e6).toFixed(1)} MB (> ${MAX_BYTES / 1e6} MB) — Thin-Client-Versprechen verletzt.`);
  process.exit(1);
}
const src = readFileSync("main.js", "utf8");
const hit = FORBIDDEN.find((re) => re.test(src));
if (hit) {
  console.error(`check-clean: verbotenes Muster im Bundle: ${hit}`);
  process.exit(1);
}
console.log(`check-clean: ok (${(size / 1024).toFixed(0)} KB, keine System-Fähigkeiten im Bundle)`);
```

  *(Hinweis: `check-clean.mjs` selbst nutzt `node:fs` — das ist ein Build-Script, kein Plugin-Code; es landet nie in main.js. Falls `/\beval\(/` auf legitimen Bundle-Code anschlägt (z. B. Substring in Minified-Namen), Muster auf `/[^.\w]eval\(/` schärfen und im Script-Kommentar begründen.)*
- [ ] **Step 2: `npm run gate`** → grün inkl. neuem Check.
- [ ] **Step 3: Commit** — `build(gate): check-clean — Bundle-Größe + Fähigkeits-Grep gegen Store-Scanner-Rückfall (Spec §7)`

---

### Task 10: README, manifest, CHANGELOG, Version

**Files:**
- Modify: `README.md`, `manifest.json`, `package.json`, `versions.json`, `CHANGELOG.md`

- [ ] **Step 1: README neu schreiben** (Identität: „product layer over your local image server"; explizite `## Installation`- und `## Usage`-Abschnitte — Scanner-Heuristik; Setup-Anleitungen: Draw Things (Mac App Store → Settings → API Server aktivieren, Port 7860) prominent, dann A1111 `--api`/Forge/SD.Next; Abschnitt „How network and storage are used" NEU: einzige Netzverbindung ist der selbst konfigurierte lokale Endpoint, keine Downloads, keine Telemetrie, Bilder/Prompts verlassen die Maschine nicht; Hinweis auf Legacy-Gewichte-Löschung für Upgrader).
- [ ] **Step 2: manifest.json** — `description` neu, ohne „Obsidian", Title-Case-Name bleibt: z. B. `"Generate images with your local Draw Things or A1111-compatible server — private, on-device, no cloud."`; `package.json`-`description` angleichen.
- [ ] **Step 3: CHANGELOG** — 0.5.0-Eintrag (Breaking: In-Process-Generierung + mflux entfernt; Neu: A1111-Thin-Client, Negativ-Prompt, CFG; Migration: Historie bleibt, alte Gewichte per Settings löschbar).
- [ ] **Step 4: Version 0.5.0** — wie bei 0.4.4: `npm run version-bump` (bzw. das dortige Muster: manifest/package/versions.json synchron).
- [ ] **Step 5: Gate + Commit** — `docs(release): 0.5.0 — Thin-Client-Identität in README/manifest/CHANGELOG`

---

## Abschluss (nach Task 10)

1. **Whole-Branch-Review** (Repo-Muster: frischer Reviewer über den gesamten Diff, Fokus: Spec-Abdeckung, tote Reste, i18n-EN/DE-Parität, ViewHost-Signatur-Konsistenz).
2. **Deploy zum Smoke:** `OBSIDIAN_PLUGIN_DIR=<Pallas>/.obsidian/plugins/local-image-generator npm run deploy` — Jay smoke-testet gegen Draw Things (echter Server), erst danach Release.
3. **Release 0.5.0** (etablierte Kette: Codeberg-Tag → GitHub-Mirror → Action/Attestation) und Store-PR aktualisieren — **manueller Jay-Schritt.**

## Bewusste Spec-Abweichungen

- `png.ts` stirbt nicht ganz: `dataUrlToBytes` bleibt (Task 5) — das Speichern braucht es weiterhin.
- Progress-Polling zählt `elapsedSec` im selben Tick mit (ein Timer statt zwei).
